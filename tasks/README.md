# 任务入口说明

本目录用于承载仓库级任务清单和阶段性任务入口。

## 使用方式
- 仓库级改造：直接在 `tasks/` 下新增一个任务文件。
- 功能级改造：可以在 `docs/<feature>/` 下维护更详细的 spec/tasks，但需要在本目录补一个入口说明。
- 每次开始任务前，默认阅读本目录下全部 `.md` 文件。

## 命名建议
- `00-`：基础设施或仓库级任务
- `10-`：核心功能
- `20-`：优化/专项
- `90-`：长期待办或债务
