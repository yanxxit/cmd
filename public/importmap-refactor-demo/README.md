# importmap-refactor-demo

这是一个真实可访问的示例页面，用于展示如何把“复杂单 HTML 页面”拆分为多文件原生 ESM 模块。

## 页面目标
- 说明为什么复杂 HTML 会难维护。
- 展示推荐的模块拆分方式。
- 提供简单交互，模拟拆分前后结构变化。

## 访问方式
启动本地静态服务后访问：

- `http://127.0.0.1:3000/importmap-refactor-demo/`
- 或使用任意 HTTP 服务打开 `public/` 目录后访问对应路径。
